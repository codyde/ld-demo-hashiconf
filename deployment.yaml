apiVersion: v1
kind: Namespace
metadata:
  name: hashiconf-2
  namespace: hashiconf-2
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hashiconf-2
  namespace: hashiconf-2
  labels:
    app: hashiconf-2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hashiconf-2
  template:
    metadata:
      labels:
        app: hashiconf-2
    spec:
      containers:
        - name: hashiconf-2
          image: codydearkland/ld-demo-hashiconf:2
          imagePullPolicy: Always
          ports:
            - name: web
              containerPort: 3000
              protocol: TCP
---
apiVersion: v1
kind: Service
metadata:
  namespace: hashiconf-2
  name: hashiconf-lb
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: hashiconf-2
  type: LoadBalancer